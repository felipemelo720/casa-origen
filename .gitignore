node_modules
.pnp
.pnp.js

.next
# Respaldo del build anterior que crea scripts/deploy.sh. Si un deploy muere
# entre el respaldo y la limpieza final, este directorio queda untracked y
# todos los deploys siguientes mueren en `working tree is dirty`.
.next.prev
out
build
dist
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env.production
.env*.local

coverage
.nyc_output

# Reportes JSON de vitest. Solo los escribe CI (para que el aviso de Telegram
# pueda nombrar los tests rojos), pero correr el mismo comando a mano los deja
# acá y ensucia el árbol.
unit-results.json
integration-results.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.DS_Store
*.pem

public/uploads/*
!public/uploads/.gitkeep

backups/*
!backups/.gitkeep

.vercel
.idea
.vscode/*
!.vscode/extensions.json

# Backups de env creados a mano antes de tocar produccion.
.env*.bak-*

# Base desechable de los tests de integración. Copiar a `.env.test` y
# ajustar la contraseña a la del contenedor local.
.env.test
